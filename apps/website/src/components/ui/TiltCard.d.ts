import { type ReactNode } from "react";
interface TiltCardProps {
    children: ReactNode;
    className?: string;
    maxTilt?: number;
}
export declare function TiltCard({ children, className, maxTilt }: TiltCardProps): import("react").JSX.Element;
export {};
//# sourceMappingURL=TiltCard.d.ts.map